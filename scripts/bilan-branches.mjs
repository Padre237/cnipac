#!/usr/bin/env node
/** bilan-branches — bilan d'hygiene du depot. Palier 0 : squelette, contrat d'appel fige. */
import { info, arguments_ } from './_lib.mjs';
const args = arguments_();
info('[bilan-branches] Palier 0 — non implemente. Arguments : ' + JSON.stringify(args));
