#!/usr/bin/env node
/** analyser-resultats-charge — bilan d'hygiene du depot. Palier 0 : squelette, contrat d'appel fige. */
import { info, arguments_ } from './_lib.mjs';
const args = arguments_();
info('[analyser-resultats-charge] Palier 0 — non implemente. Arguments : ' + JSON.stringify(args));
