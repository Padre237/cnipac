#!/usr/bin/env bash
# migrer.sh — squelette du Palier 0.
# A implementer au Sprint 5 (SDD §29.2) avec l'acces effectif aux hotes CENADI.
# Le contrat d'appel est fige par les workflows .github/workflows/cd-*.yml et
# ne doit pas changer : seule l'implementation reste a ecrire.
set -Eeuo pipefail
echo "[migrer.sh] non implemente — Palier 0. Arguments recus : $*" >&2
exit 0
